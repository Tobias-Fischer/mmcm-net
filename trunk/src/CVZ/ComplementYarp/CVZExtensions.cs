using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Complement_Yarp
{
    static class CVZExtensions
    {
	   //public static bool GetVisualization(this CVZ_Core.CVZ_MMCM map, int layer, ref ImageRgb img)
	   //{
	   //    img
	   //    return HelpersLib.ImageManipulation.toImg(GetVisualization(layer), ref img);
	   //}
    }
}
