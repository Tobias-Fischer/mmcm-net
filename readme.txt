In order for MMCM to work you need environment variables set :
MMCM_ROOT should point to this directory
MMCM_BIN should point to the directory where are copied the binaries and be added to the path (usually MMCM_DIR/bin)